# HabitFlow 🕒
A simple full-stack habit tracker app built with Node.js, Express, MongoDB, and vanilla JavaScript.

## Features
- User authentication
- Track daily habits
- View progress over time
- Responsive design

## Setup
1. Clone the repo
2. Run `npm install` in `/backend`
3. Run the backend server with `node server.js`